name: widget_to_image_example
description: Demonstrates how to use the widget_to_image package.

version: 1.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  widget_to_image:
    path: ../

flutter:
  uses-material-design: true
